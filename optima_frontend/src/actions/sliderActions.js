export const previousSlide = () => {
   return (
      {
         type: 'PREVIOUS_SLIDE',
      }
   );
};

export const nextSlide = () => {
   return (
      {
         type: 'NEXT_SLIDE',
      }
   );
};
